
import { merriweather_sans } from "@/app/fonts/fonts"

export const FIltersComponent = () => {
    return (
        <div className={`border border-zinc-300 rounded-xl w-[90%] mx-auto h-32 bg-zinc-100 p-10 ${merriweather_sans.className}`}>
            <h1>En esta seccion irian los filtros</h1>
        </div>
    )
}