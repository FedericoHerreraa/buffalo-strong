



export async function POST() {
    // const { title, description, sugestedPrice, listPrice, brand, img, listCode, category, color, stock, group, subcategory } = await req.json();

    // Subir la imagen a storage de supabase

    // Sacar el id por 5 años

    // Crear el producto en la base de datos con el id de la imagen

    // Retornar el status de la operacion

    return Response.json({ message: "Funcionalidad no implementada" });
}